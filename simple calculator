function calculator(a: number, b: number, op: string): number | string {
    if (op === "+") return a + b;
    if (op === "-") return a - b;
    if (op === "*") return a * b;
    if (op === "/") return a / b;
    return "Invalid operation";
}

console.log(calculator(5, 2, "+")); // 7
